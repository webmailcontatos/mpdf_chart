<svg
        width="{{ width }}"
        height="{{ height }}"
        viewBox="{{ defaultWidth}} {{ defaultHeight }} {{ width }} {{ height }}">
    <defs>
        {{ gradient|raw }}
    </defs>
    <polygon
            points="{{ points }}"
            opacity="{{ opacity }}"
            fill="url(#grad1)"
    />
</svg>
